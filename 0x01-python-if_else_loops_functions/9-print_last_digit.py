#!/usr/bin/python3
def print_last_digit(number):
#    if not (number is None):
#        lasdig = abs(number) % 10
#    return(lasdig)
    lasdig = (abs(number) % 10) 
    if number != 0:
#        lasdig = (abs(number) % 10)
        return(lasdig)
