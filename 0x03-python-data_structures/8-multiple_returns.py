#!/usr/bin/python3
def multiple_returns(sentence):

    leng = len(sentence)
    buff = sentence[0]

    if sentence == []:
        buff == 0
    else:
        buff == sentence[0]
    return(leng, buff)
