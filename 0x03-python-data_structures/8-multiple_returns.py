#!/usr/bin/python3
def multiple_returns(sentence):

    leng = len(sentence)
    buff = sentence[0]

    if leng == []:
        buff == None
    else:
        buff == sentence[0]
    return(leng, buff)
